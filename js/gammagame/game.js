// * Import libs and things ↓:
fs = require("fs");
rl = require("readline").createInterface({
  input: process.stdin,
  output: process.stdout
});
data = require("./data.json");
require("./commands.js");
